# Supabase Public (Template only reads data)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=ey...

# Site Identity (Injected by Vercel during deployment, but needed for local dev)
SITE_ID=uuid-of-a-site-from-database

# Brain API Key (For handshake if implemented)
BRAIN_API_KEY=some-random-secret
